# 🚀 Быстрый запуск проекта

## Способ 1: Автоматический запуск (рекомендуется)

Используйте скрипт `start.sh`, который автоматически настроит всё необходимое:

```bash
chmod +x start.sh
./start.sh
```

Этот скрипт автоматически:
- ✅ Создаст `.env` из `env.sample` (если его нет)
- ✅ Определит Django модуль
- ✅ Выполнит миграции
- ✅ Соберет Docker образы
- ✅ Запустит контейнеры
- ✅ Настроит `ALLOWED_HOSTS`
- ✅ Применит необходимые патчи к `settings.py`

**После запуска:**
- Приложение будет доступно по адресу: http://localhost:8000
- Демо-данные (пользователи, новости, транзакции) создадутся автоматически при первом запуске

---

## Способ 2: Прямой запуск через Docker Compose

Если вы уже настроили проект ранее:

```bash
# Просто запустите контейнеры
docker compose up -d

# Или с пересборкой образов (если изменили код)
docker compose up -d --build
```

**Важно:** При первом запуске убедитесь, что:
1. Файл `.env` существует (скопируйте из `env.sample` если нужно)
2. В `.env` установлен `SECRET_KEY` для Django

---

## Что происходит при запуске

### При первом запуске контейнера `app`:

1. **Ожидание PostgreSQL** - контейнер ждет, пока база данных будет готова
2. **Миграции** - автоматически выполняются `python manage.py migrate`
3. **Статика** - собираются статические файлы `python manage.py collectstatic`
4. **Демо-данные** - автоматически запускается `python init_data.py`, который создает:
   - Пользователей (operator1, operator2, client1-client5)
   - Новости для бегущей строки (100+ новостей)
   - Услуги и категории
   - Банковские карты
   - **110+ транзакций** (переводы, платежи, пополнения, снятия, комиссии)
   - Депозиты, кредиты, инвестиции
5. **Gunicorn** - запускается веб-сервер на порту 8000

---

## Полезные команды

### Просмотр логов
```bash
# Все логи
docker compose logs -f

# Только логи приложения
docker compose logs -f app

# Только логи базы данных
docker compose logs -f db
```

### Остановка
```bash
# Остановить контейнеры
docker compose stop

# Остановить и удалить контейнеры
docker compose down

# Остановить и удалить контейнеры + volumes (⚠️ удалит данные БД!)
docker compose down -v
```

### Перезапуск
```bash
# Перезапустить контейнеры
docker compose restart

# Перезапустить только приложение
docker compose restart app
```

### Выполнение команд в контейнере
```bash
# Django shell
docker compose exec app python manage.py shell

# Создать суперпользователя
docker compose exec app python manage.py createsuperuser

# Выполнить миграции вручную
docker compose exec app python manage.py migrate

# Пересоздать демо-данные
docker compose exec app python init_data.py
```

### Проверка статуса
```bash
# Статус контейнеров
docker compose ps

# Проверка здоровья контейнеров
docker compose ps --format "table {{.Name}}\t{{.Status}}\t{{.Ports}}"
```

---

## Доступ к приложению

После успешного запуска:

- **Веб-интерфейс:** http://localhost:8000
- **Админ-панель:** http://localhost:8000/admin

### Тестовые аккаунты (создаются автоматически):

**Операторы:**
- `operator1` / `password123` - Оператор ДБО #1
- `operator2` / `password123` - Оператор ДБО #2

**Клиенты:**
- `client1` / `password123` - Петр Иванов
- `client2` / `password123` - Мария Смирнова
- `client3` / `password123` - Алексей Козлов
- `client4` / `password123` - Елена Морозова
- `client5` / `password123` - Дмитрий Волков

---

## Устранение проблем

### Контейнер не запускается
```bash
# Проверьте логи
docker compose logs app

# Проверьте статус
docker compose ps
```

### База данных не подключается
```bash
# Проверьте, что контейнер БД запущен
docker compose ps db

# Проверьте логи БД
docker compose logs db

# Пересоздайте БД (⚠️ удалит все данные!)
docker compose down -v
docker compose up -d
```

### Порты заняты
Если порт 8000 занят, измените в `docker-compose.yml`:
```yaml
ports:
  - "8001:8000"  # Используйте другой порт
```

### Демо-данные не создались
```bash
# Запустите вручную
docker compose exec app python init_data.py

# Проверьте логи при запуске
docker compose logs app | grep "Создание демо"
```

---

## Структура проекта

```
dbo/
├── docker-compose.yml    # Конфигурация Docker Compose
├── Dockerfile            # Образ приложения
├── start.sh             # Скрипт автоматического запуска
├── init_data.py         # Скрипт создания демо-данных
├── .env                 # Переменные окружения (создается автоматически)
├── env.sample           # Шаблон переменных окружения
└── ...
```

---

## Рекомендации

1. **Первый запуск:** Используйте `./start.sh` - он настроит всё автоматически
2. **Последующие запуски:** Используйте `docker compose up -d` - быстрее
3. **После изменений в коде:** Используйте `docker compose up -d --build` для пересборки
4. **Для разработки:** Можете монтировать код как volume для hot-reload (не настроено по умолчанию)

---

## Что дальше?

После запуска вы можете:
- Войти в систему под одним из тестовых аккаунтов
- Просмотреть транзакции: http://localhost:8000/client/transactions/
- Управлять картами: http://localhost:8000/cards/
- Просмотреть депозиты: http://localhost:8000/deposits/
- И многое другое!

