# Исправление бегущей строки новостей в Docker

## Проблема
Бегущая строка новостей не работает в Docker контейнере.

## Решения

### 1. Убедитесь, что в базе данных есть новости

Проверьте наличие новостей:
```bash
docker compose exec app python manage.py shell
```

В Python shell:
```python
from dbo.models import News
print(f"Всего новостей: {News.objects.count()}")
print(f"Активных новостей: {News.objects.filter(is_active=True).count()}")
```

Если новостей нет, создайте их:
```bash
# Вариант 1: Установите INIT_DATA=1 в .env и перезапустите
docker compose down
# Отредактируйте .env: INIT_DATA=1
docker compose up -d

# Вариант 2: Запустите вручную
docker compose exec app python init_data.py
```

### 2. Проверьте статические файлы

Убедитесь, что статические файлы собраны:
```bash
docker compose exec app ls -la staticfiles/
```

Если папка пуста или отсутствует:
```bash
docker compose exec app python manage.py collectstatic --noinput
```

### 3. Проверьте логи контейнера

```bash
docker compose logs app | grep -i "news\|static\|ticker"
```

### 4. Пересоберите контейнер

После изменений в requirements.txt или settings.py:
```bash
docker compose down
docker compose build --no-cache
docker compose up -d
```

### 5. Проверьте в браузере

1. Откройте консоль разработчика (F12)
2. Проверьте вкладку Console на наличие ошибок JavaScript
3. Проверьте вкладку Network - загружаются ли статические файлы (CSS/JS)?
4. Проверьте, что элемент `#news-ticker` присутствует в DOM

### 6. Ручная проверка новостей через админку

```bash
# Создайте суперпользователя если его нет
docker compose exec app python manage.py createsuperuser

# Затем откройте http://localhost:8000/admin/
# Перейдите в раздел "Новости" и создайте несколько активных новостей
```

## Что было исправлено

1. ✅ Добавлен WhiteNoise для обслуживания статических файлов в продакшене
2. ✅ Обновлен requirements.txt (добавлен whitenoise>=6.6.0)
3. ✅ Обновлен settings.py (добавлен WhiteNoiseMiddleware)
4. ✅ Обновлен urls.py (статики обслуживаются через WhiteNoise в продакшене)
5. ✅ Улучшен Dockerfile entrypoint (проверка новостей при запуске)
6. ✅ Улучшен сбор статических файлов (проверка успешности)

## После исправлений

Пересоберите и перезапустите:
```bash
docker compose down
docker compose build
docker compose up -d
docker compose logs -f app
```

Ожидаемый вывод в логах:
```
📰 Checking news in database...
  Active news in database: X
  ✓ News ticker will display X news items
```

