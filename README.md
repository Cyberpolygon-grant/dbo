# Система ДБО "ФинансПро"

Веб-приложение для дистанционного банковского обслуживания (ДБО), реализованное на Django. Система предоставляет клиентам возможность управлять банковскими операциями онлайн, а операторам - обрабатывать заявки и управлять клиентской базой.

## 📋 Описание

Система ДБО "ФинансПро" - это полнофункциональная платформа для интернет-банкинга, включающая:

- **Управление банковскими картами** - просмотр балансов, блокировка/разблокировка карт, смена PIN-кода
- **Переводы и платежи** - переводы между картами, оплата услуг, проверка получателей
- **Депозиты и инвестиции** - открытие депозитов, инвестиционные продукты
- **Каталог услуг** - просмотр и подключение банковских услуг
- **Система заявок** - создание заявок на новые услуги с модерацией операторами
- **Роли пользователей**:
  - **Клиенты** - доступ к личному кабинету и банковским операциям
  - **Оператор ДБО #1** - создание клиентов, просмотр логов, управление клиентской базой
  - **Оператор ДБО #2** - модерация заявок на услуги, одобрение/отклонение заявок

## 🏗️ Архитектура

### Технологический стек

- **Backend**: Django 5.2+
- **Frontend**: daisyUI 5 + Tailwind CSS 4
- **База данных**: PostgreSQL 16 (production) / SQLite (development)
- **WSGI сервер**: Gunicorn
- **Контейнеризация**: Docker + Docker Compose
- **Автоматизация**: Python боты (Playwright, requests)

### Структура проекта

```
dbo/
├── cyberpolygon/              # Настройки Django проекта
│   ├── settings.py           # Конфигурация приложения
│   ├── urls.py               # Главный URL роутер
│   ├── wsgi.py               # WSGI конфигурация
│   └── asgi.py               # ASGI конфигурация
│
├── dbo/                      # Основное приложение
│   ├── models.py             # Модели данных (Client, Operator, Service, Transaction и др.)
│   ├── views.py              # Представления (views)
│   ├── admin.py              # Админ-панель Django
│   ├── context_processors.py # Контекстные процессоры (новости)
│   ├── logging_helper.py     # Хелпер для логирования
│   │
│   ├── templates/            # HTML шаблоны
│   │   ├── base.html        # Базовый шаблон
│   │   ├── index.html       # Главная страница
│   │   ├── login.html       # Страница входа
│   │   ├── client_dashboard.html      # Дашборд клиента
│   │   ├── operator1_dashboard.html  # Дашборд оператора #1
│   │   ├── operator2_dashboard.html  # Дашборд оператора #2
│   │   ├── review_service_request.html  # Просмотр заявки
│   │   └── services/        # Шаблоны сервисов
│   │
│   ├── management/          # Кастомные команды управления
│   │   └── commands/
│   │       ├── init_banking_services.py    # Инициализация банковских услуг
│   │       ├── check_users.py              # Проверка пользователей
│   │       ├── check_news_stats.py         # Статистика новостей
│   │       ├── clear_demo_data.py          # Очистка демо-данных
│   │       └── expose_privileged_services.py  # Экспорт привилегированных услуг
│   │
│   ├── migrations/          # Миграции базы данных
│   └── templatetags/       # Кастомные теги шаблонов
│
├── static/                  # Статические файлы
│   ├── css/                # Стили (daisyUI, Tailwind)
│   └── js/                 # JavaScript файлы
│
├── manage.py               # Django CLI утилита
├── requirements.txt        # Зависимости Python
│
├── Dockerfile              # Docker образ для приложения
├── Dockerfile.bot          # Docker образ для бота оператора #2
├── Dockerfile.user_bot     # Docker образ для бота пользователя
├── docker-compose.yml      # Docker Compose конфигурация
│
├── dbo_user_bot.py         # Бот пользователя (создает транзакции, заявки)
├── operator2_bot.py        # Бот оператора #2 (обрабатывает заявки через Playwright)
├── init_data.py            # Скрипт инициализации демо-данных
│
└── README.md               # Этот файл
```

## 🚀 Быстрый старт

### Требования

- Docker и Docker Compose
- Или Python 3.11+ и PostgreSQL (для локальной разработки)

### Запуск через Docker Compose (рекомендуется)

```bash
# Клонирование репозитория
git clone <repository-url>
cd dbo

# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f app

# Остановка
docker-compose down
```

Приложение будет доступно по адресу: `http://localhost:8000`

### Локальная разработка

```bash
# Установка зависимостей
pip install -r requirements.txt

# Настройка базы данных (PostgreSQL или SQLite)
# Для PostgreSQL создайте базу данных и настройте переменные окружения:
export POSTGRES_DB=appdb
export POSTGRES_USER=appuser
export POSTGRES_PASSWORD=apppass
export POSTGRES_HOST=localhost
export POSTGRES_PORT=5432

# Применение миграций
python manage.py migrate

# Создание суперпользователя
python manage.py createsuperuser

# Инициализация демо-данных
python init_data.py

# Запуск сервера разработки
python manage.py runserver
```

## 🔐 Учетные записи по умолчанию

После инициализации демо-данных (`init_data.py`) создаются следующие пользователи:

### Клиенты
- **Email**: `client1@example.com` / **Password**: `1q2w#E$R`
- **Email**: `client2@example.com` / **Password**: `1q2w#E$R`

### Операторы
- **Оператор ДБО #1**: `operator1@financepro.ru` / **Password**: `1q2w#E$R%T`
- **Оператор ДБО #2**: `operator2@financepro.ru` / **Password**: `1q2w#E$R%T`

## 📦 Docker сервисы

Проект включает следующие Docker контейнеры:

### `app` - Основное приложение Django
- Порт: `8000`
- Зависимости: `db` (PostgreSQL)
- Автоматически применяет миграции и инициализирует данные при первом запуске

### `db` - PostgreSQL база данных
- Порт: `5432` (внутренний)
- Версия: PostgreSQL 16 Alpine
- Данные сохраняются в Docker volume `db_data`

### `operator2-bot` - Бот оператора #2
- Автоматически обрабатывает заявки на услуги
- Использует Playwright для эмуляции браузера
- Периодически проверяет новые заявки (интервал: 30 секунд)

### `dbo-user-bot` - Бот пользователя
- Создает транзакции и заявки на услуги
- Эмулирует активность пользователей
- Использует Django ORM напрямую

## 🎯 Основные функции

### Для клиентов

1. **Личный кабинет** (`/client/`)
   - Просмотр балансов карт
   - История транзакций
   - Управление услугами

2. **Переводы** (`/client/transfers/`)
   - Переводы между картами
   - Проверка получателя по номеру телефона
   - История переводов

3. **Депозиты** (`/client/deposits/`)
   - Просмотр активных депозитов
   - Открытие новых депозитов

4. **Инвестиции** (`/investments/`)
   - Просмотр инвестиционных продуктов
   - Открытие инвестиционных счетов

5. **Карты** (`/cards/`)
   - Просмотр всех карт
   - Блокировка/разблокировка
   - Смена PIN-кода
   - Установка основной карты

6. **Каталог услуг** (`/banking-services/`)
   - Просмотр доступных услуг
   - Подключение услуг
   - Создание заявок на новые услуги

### Для оператора ДБО #1

1. **Дашборд** (`/operator1/`)
   - Статистика по клиентам
   - Список новых клиентов

2. **Создание клиентов** (`/create-client/`)
   - Регистрация новых клиентов
   - Назначение банковских карт

3. **Логи** (`/operator1/logs/`)
   - Просмотр системных логов
   - Фильтрация по типу событий

### Для оператора ДБО #2

1. **Дашборд** (`/operator2/`)
   - Список заявок на услуги
   - Статистика по заявкам

2. **Просмотр заявки** (`/review-request/<id>/`)
   - Детальная информация о заявке
   - Описание услуги (может содержать XSS)
   - Одобрение/отклонение заявки

3. **Одобрение заявки** (`/approve-request/<id>/`)
   - POST-запрос для одобрения заявки

## 🤖 Автоматизация

### Бот оператора #2 (`operator2_bot.py`)

Автоматически обрабатывает заявки на услуги:

- Авторизуется как `operator2@financepro.ru`
- Периодически проверяет новые заявки (интервал: 30 секунд)
- Открывает страницу заявки через Playwright
- Выполняет JavaScript на странице (для тестирования XSS)
- Автоматически переходит по ссылкам в описании заявки (если есть)

**Настройки через переменные окружения:**
- `APP_URL` - URL приложения (по умолчанию: `http://app:8000`)
- `BOT_USERNAME` - Имя пользователя бота
- `BOT_PASSWORD` - Пароль бота
- `CHECK_INTERVAL` - Интервал проверки заявок в секундах (по умолчанию: 30)
- `USE_HEADFUL_BROWSER` - Использовать реальный браузер вместо headless (по умолчанию: `false`)

### Бот пользователя (`dbo_user_bot.py`)

Эмулирует активность пользователей:

- Создает случайные транзакции
- Создает заявки на услуги с реалистичными описаниями
- Подключает услуги
- Использует Django ORM напрямую

## 🗄️ Модели данных

### Основные модели

- **Client** - Клиенты банка
- **Operator** - Операторы ДБО (типы: client_service, security)
- **Service** - Банковские услуги
- **ServiceRequest** - Заявки на создание новых услуг
- **ClientService** - Подключенные услуги клиентов
- **BankCard** - Банковские карты
- **Transaction** - Банковские транзакции
- **Deposit** - Депозиты
- **InvestmentProduct** - Инвестиционные продукты
- **ClientInvestment** - Инвестиции клиентов
- **News** - Новости для бегущей строки
- **DBOLog** - Системные логи

## 🔧 Управление

### Django команды

```bash
# Инициализация банковских услуг
python manage.py init_banking_services

# Проверка пользователей
python manage.py check_users

# Статистика новостей
python manage.py check_news_stats

# Очистка демо-данных
python manage.py clear_demo_data

# Экспорт привилегированных услуг
python manage.py expose_privileged_services
```

### Переменные окружения

```bash
# База данных
POSTGRES_HOST=db
POSTGRES_PORT=5432
POSTGRES_DB=appdb
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppass

# Django
DJANGO_SETTINGS_MODULE=cyberpolygon.settings
ALLOWED_HOSTS=*
SECRET_KEY=your-secret-key

# Приложение
DEFAULT_NEW_CLIENT_PASSWORD=1q2w#E$R

# Бот оператора #2
APP_URL=http://app:8000
BOT_USERNAME=operator2@financepro.ru
BOT_PASSWORD=1q2w#E$R%T
CHECK_INTERVAL=30
USE_HEADFUL_BROWSER=false
```

## 🛠️ Разработка

### Структура кода

- **Модели** (`dbo/models.py`) - Определение структуры данных
- **Представления** (`dbo/views.py`) - Обработка HTTP-запросов
- **Шаблоны** (`dbo/templates/`) - HTML-шаблоны с daisyUI компонентами
- **URL-маршруты** (`cyberpolygon/urls.py`) - Маршрутизация запросов

### Добавление новых функций

1. Создайте модель в `dbo/models.py`
2. Создайте миграцию: `python manage.py makemigrations`
3. Примените миграцию: `python manage.py migrate`
4. Создайте представление в `dbo/views.py`
5. Добавьте URL-маршрут в `cyberpolygon/urls.py`
6. Создайте шаблон в `dbo/templates/`

## 📝 Логирование

Система использует Django logging для записи событий:

- **DBOLog** - Высокоуровневые события (входы, создание клиентов и т.д.)
- **Gunicorn logs** - Логи веб-сервера (в `/app/logs/` в Docker)
- **Console logs** - Логи приложения в консоль

## 🔒 Безопасность

⚠️ **Важно**: Это демонстрационное приложение для обучения и тестирования. Оно содержит уязвимости намеренно для демонстрации:

- SQL-инъекции в поиске услуг
- XSS в описаниях заявок
- Потенциальные проблемы с авторизацией

**Не используйте в production без исправления уязвимостей!**

## 📄 Лицензия

Этот проект предназначен для образовательных целей.

## 🤝 Поддержка

Для вопросов и предложений создайте issue в репозитории проекта.

## 📚 Дополнительная документация

- [Django Documentation](https://docs.djangoproject.com/)
- [daisyUI Documentation](https://daisyui.com/)
- [Docker Documentation](https://docs.docker.com/)
- [Playwright Documentation](https://playwright.dev/python/)
